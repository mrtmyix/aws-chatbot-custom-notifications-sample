{
    "name": "terraform devcontainer",
    "dockerComposeFile": ["../compose.yaml"],
    "service": "terraform",
    "workspaceFolder": "/work",
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
                "hashicorp.terraform"
            ]
        }
    }
}
